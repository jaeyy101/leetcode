/**
 * @param {string} s
 * @return {string}
 */
var reverseWords = function (s) {
  let result = "";

  let slow = s.length - 1;
  let fast = s.length - 1;

  function reverse(start, end) {
    while (start < end) {}
  }
  while (fast >= 0) {}
};
